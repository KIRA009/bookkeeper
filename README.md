# Bookkeeper

### Installation
- Clone the repository
- Run `npm install`

### Running the app
- Copy the `.env.example` file to `.env` and fill in the required environment variables
- Run `npm run dev`
